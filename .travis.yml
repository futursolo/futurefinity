language: python
python:
  - "3.5"
  - "nightly"
install:
  - pip install .
  - pip install coveralls

services:
  - redis-server

# command to run tests
script: coverage run --source=futurefinity,futurefinity.interface setup.py test

after_success:
  coveralls
